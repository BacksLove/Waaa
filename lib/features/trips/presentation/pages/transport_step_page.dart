import 'package:flutter/material.dart';

class TransportStepPage extends StatelessWidget {
  const TransportStepPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
